import{_ as a,o as s,c as n,a0 as o}from"./chunks/framework.CNDWyMYS.js";const g=JSON.parse('{"title":"luarocks config","description":"","frontmatter":{},"headers":[],"relativePath":"luarocks_config.md","filePath":"luarocks_config.md"}'),i={name:"luarocks_config.md"};function t(l,e,c,p,r,u){return s(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="luarocks-config" tabindex="-1">luarocks config <a class="header-anchor" href="#luarocks-config" aria-label="Permalink to &quot;luarocks config&quot;">​</a></h1><p>Query information about the LuaRocks configuration.</p><h1 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h1><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks config (&lt;key&gt; | &lt;key&gt; &lt;value&gt; --scope=&lt;scope&gt; | &lt;key&gt; --unset --scope=&lt;scope&gt; | )</span></span></code></pre></div><p>When given a configuration key, it prints the value of that key according to the currently active configuration (taking into account all config files and any command-line flags passed)</p><p>Examples:</p><ul><li><code>luarocks config lua_interpreter</code></li><li><code>luarocks config variables.LUA_INCDIR</code></li><li><code>luarocks config lua_version</code></li></ul><p>When given a configuration key and a value, it overwrites the config file (see the --scope option below to determine which) and replaces the value of the given key with the given value.</p><ul><li><code>lua_dir</code> is a special key as it checks for a valid Lua installation (equivalent to --lua-dir) and sets several keys at once.</li><li><code>lua_version</code> is a special key as it changes the default Lua version used by LuaRocks commands (equivalent to passing --lua-version).</li></ul><p>Examples:</p><ul><li><code>luarocks config variables.OPENSSL_DIR /usr/local/openssl</code></li><li><code>luarocks config lua_dir /usr/local</code></li><li><code>luarocks config lua_version 5.3</code></li></ul><p>When given a configuration key and --unset, it overwrites the config file (see the --scope option below to determine which) and deletes that key from the file.</p><p>Example: <code>luarocks config variables.OPENSSL_DIR --unset</code></p><p>When given no arguments, it prints the entire currently active configuration, resulting from reading the config files from all scopes.</p><p>Example: <code>luarocks config</code></p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--scope=&lt;scope&gt;   The scope indicates which config file should be rewritten.</span></span>
<span class="line"><span>                   Accepted values are &quot;system&quot;, &quot;user&quot; or &quot;project&quot;.</span></span>
<span class="line"><span>                   * Using a wrapper created with \`luarocks init\`,</span></span>
<span class="line"><span>                     the default is &quot;project&quot;.</span></span>
<span class="line"><span>                   * Using --local (or when \`local_by_default\` is \`true\`),</span></span>
<span class="line"><span>                     the default is &quot;user&quot;.</span></span>
<span class="line"><span>                   * Otherwise, the default is &quot;system&quot;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>--json           Output as JSON</span></span></code></pre></div>`,17)])])}const h=a(i,[["render",t]]);export{g as __pageData,h as default};
