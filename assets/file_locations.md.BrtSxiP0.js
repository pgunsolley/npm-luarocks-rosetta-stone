import{_ as a,o,c as t,a0 as i}from"./chunks/framework.CNDWyMYS.js";const p=JSON.parse('{"title":"File locations","description":"","frontmatter":{},"headers":[],"relativePath":"file_locations.md","filePath":"file_locations.md"}'),l={name:"file_locations.md"};function s(r,e,n,u,c,d){return o(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="file-locations" tabindex="-1">File locations <a class="header-anchor" href="#file-locations" aria-label="Permalink to &quot;File locations&quot;">​</a></h1><p>This is a summary of all file paths related to LuaRocks, including the files that compose LuaRocks (its scripts and modules), files installed by LuaRocks (Lua modules and scripts), and files required by LuaRocks (programs, libraries and headers).</p><p>For each path, the default value is also listed.</p><p>Whenever &quot;5.x&quot; is used, it refers to the Lua version you configured during installation.</p><h1 id="path-to-luarocks" tabindex="-1">Path to LuaRocks <a class="header-anchor" href="#path-to-luarocks" aria-label="Permalink to &quot;Path to LuaRocks&quot;">​</a></h1><ul><li><p>LuaRocks command-line scripts. These are the main LuaRocks commands (<code>luarocks</code>, <code>luarocks-admin</code>); it should be in your system PATH.</p><ul><li>Unix default: /usr/local/bin</li></ul></li><li><p>LuaRocks modules. These are Lua modules (<code>luarocks.fs</code>, etc.) used by LuaRocks. The LuaRocks command-line tools are configured during installation to be able to find those files.</p><ul><li>Unix default: /usr/local/share/lua/5.x/</li></ul></li></ul><h1 id="path-to-lua-binaries-and-associated-data" tabindex="-1">Path to Lua binaries and associated data <a class="header-anchor" href="#path-to-lua-binaries-and-associated-data" aria-label="Permalink to &quot;Path to Lua binaries and associated data&quot;">​</a></h1><ul><li><p>Lua interpreter. Where to find the Lua interpreter to execute scripts and LuaRocks itself.</p><ul><li>Unix default: /usr/local/bin/lua</li></ul></li><li><p>Lua libraries. The directory containing the Lua virtual machine as a shared library. On some platforms, Lua modules written in C (also called &quot;C modules&quot;) should link against this library</p><ul><li>Unix default: /usr/local/lib</li></ul></li><li><p>Lua header files (lua.h, etc.). They are required when compiling C modules.</p><ul><li>Unix default: /usr/local/include</li></ul></li></ul><p>On Unix, those locations vary according to your OS variant or distribution. If you&#39;re using a Linux distribution, for example, you may have installed Lua using your package manager and paths use /usr/ rather than /usr/local/, and the <code>lua</code> binary may be called <code>lua-5.x</code>.</p><p>For Windows users, having libraries and headers around may be uncommon, as Windows tends to have binary distributions. LuaRocks can provide binary rocks, but rocks in the standard repository are often available only as source code and need to be compiled during the installation process.</p><h1 id="paths-to-rocks-trees" tabindex="-1">Paths to rocks trees <a class="header-anchor" href="#paths-to-rocks-trees" aria-label="Permalink to &quot;Paths to rocks trees&quot;">​</a></h1><p>LuaRocks by default is configured to use two rocks trees:</p><ul><li>System-wide <a href="./rocks_repositories.html">rock tree</a> (used by default) <ul><li>Unix default: /usr/local/</li></ul></li><li>Per-user <a href="./rocks_repositories.html">rock tree</a> (may be selected by the user with the --local flag). <ul><li>Unix default: $HOME/.luarocks/</li></ul></li></ul><p>In order to use the modules installed in the rock trees, the <a href="./rocks_repositories.html">deployment directories</a> should be in the <code>LUA_PATH</code> and <code>LUA_CPATH</code> environment variables.</p><p>On Unix, performing a vanilla installation of Lua from lua.org and a vanilla installation of LuaRocks will install both under /usr/local, meaning that the environment variables for the system-wide rock tree are correct by default.</p><p>The command <a href="./luarocks_path.html"><code>luarocks path</code></a> outputs the correct environment variables set for a rock tree.</p><h1 id="path-where-command-line-scripts-are-installed" tabindex="-1">Path where command-line scripts are installed <a class="header-anchor" href="#path-where-command-line-scripts-are-installed" aria-label="Permalink to &quot;Path where command-line scripts are installed&quot;">​</a></h1><p>Rocks may also deploy command-line scripts. This location is relative to the rock tree where they are installed:</p><ul><li><p>System-wide <a href="./rocks_repositories.html">rock tree</a> (used by default)</p><ul><li>Unix default: /usr/local/bin/</li></ul></li><li><p>Per-user <a href="./rocks_repositories.html">rock tree</a> (may be selected by the user with the --local flag).</p><ul><li>Unix default: $HOME/.luarocks/bin/</li></ul></li></ul><p>To be able to call those scripts from the shell, the location where they are installed need to be in your environment path.</p><p>On Unix, /usr/local/bin is usually in the $PATH by default, but $HOME/.luarocks/bin is not.</p><p>The command <a href="./luarocks_path.html"><code>luarocks path --bin</code></a> outputs the correct PATH environment variables set for a rock tree.</p><h1 id="compiler" tabindex="-1">Compiler <a class="header-anchor" href="#compiler" aria-label="Permalink to &quot;Compiler&quot;">​</a></h1><p>For compiling modules written in C, you need a compiler toolchain installed.</p><p>For many platforms this is a non-issue: Linux distribution often include <code>gcc</code> (the default installation of Ubuntu, notably, ships without build tools by default: run <code>sudo apt-get install build-essential</code>).</p><p>On Windows, where binary distribution is the standard, this might be an additional requirement. Because more rocks are available as source code than as binary rocks, it is probably best to have a C compiler available. LuaRocks supports MinGW and the Microsoft Visual Studio toolchains. The compiler should be in the system path, or explicitly configured in the LuaRocks config files.</p>',26)])])}const m=a(l,[["render",s]]);export{p as __pageData,m as default};
