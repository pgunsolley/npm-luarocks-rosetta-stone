import{_ as e,o as s,c as t,a0 as o}from"./chunks/framework.CNDWyMYS.js";const h=JSON.parse('{"title":"Using LuaRocks","description":"","frontmatter":{},"headers":[],"relativePath":"using_luarocks.md","filePath":"using_luarocks.md"}'),n={name:"using_luarocks.md"};function i(l,a,p,r,c,d){return s(),t("div",null,[...a[0]||(a[0]=[o(`<h1 id="using-luarocks" tabindex="-1">Using LuaRocks <a class="header-anchor" href="#using-luarocks" aria-label="Permalink to &quot;Using LuaRocks&quot;">​</a></h1><p>So, you have followed the installation instructions (either on <a href="./installation_instructions_for_unix.html">Unix</a> or <a href="./installation_instructions_for_windows.html">Windows</a>) and now you have LuaRocks installed on your machine. Now you probably want to install some rocks (packages containing Lua modules) and use them in your Lua code.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p><strong>Node.js Developer Quick Tip</strong>: Installing rocks with <code>luarocks install &lt;name&gt;</code> is equivalent to running <code>npm install &lt;name&gt;</code>. However, by default LuaRocks installs packages globally (similar to <code>npm install -g &lt;name&gt;</code>).</p><p>To learn how to install packages locally to your project (like a local <code>node_modules/</code> folder), see the <a href="./luarocks_for_npm_users.html">LuaRocks for npm Developers Guide</a>.</p></div><p>For LuaRocks to function properly, we have a quick checklist to go through first:</p><h1 id="command-line-tools-and-the-system-path" tabindex="-1">Command-line tools (and the system path) <a class="header-anchor" href="#command-line-tools-and-the-system-path" aria-label="Permalink to &quot;Command-line tools (and the system path)&quot;">​</a></h1><p>LuaRocks installs some command-line tools which are your interface for managing your rocks: <a href="./luarocks.html">luarocks</a> and <a href="./luarocks_admin.html">luarocks-admin</a>. Make sure the directory where they are located is in your PATH -- the exact location depends on the flags you gave when installing LuaRocks.</p><p>Run <a href="./luarocks.html">luarocks</a> to see the available commands:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks</span></span></code></pre></div><p>You can get help on any command by using the <a href="./luarocks_help.html">luarocks help</a> command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks help install</span></span></code></pre></div><p>Installing packages is done by typing commands such as:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks install dkjson</span></span></code></pre></div><h1 id="rocks-trees-and-the-lua-libraries-path" tabindex="-1">Rocks trees and the Lua libraries path <a class="header-anchor" href="#rocks-trees-and-the-lua-libraries-path" aria-label="Permalink to &quot;Rocks trees and the Lua libraries path&quot;">​</a></h1><p>When you install rocks using the <code>luarocks install</code>, you get new modules available for loading via <code>require()</code> from Lua. For example, after we install the dkjson rock, type <code>luarocks show dkjson</code> to show the module installed by the rock:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks show dkjson</span></span></code></pre></div><p>This should output something like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>dkjson 2.5-2 - David Kolf&#39;s JSON module for Lua</span></span>
<span class="line"><span>  </span></span>
<span class="line"><span>dkjson is a module for encoding and decoding JSON data. It supports UTF-8.</span></span>
<span class="line"><span>  </span></span>
<span class="line"><span>JSON (JavaScript Object Notation) is a format for serializing data based on the</span></span>
<span class="line"><span>syntax for JavaScript data structures.</span></span>
<span class="line"><span>  </span></span>
<span class="line"><span>dkjson is written in Lua without any dependencies, but when LPeg is available</span></span>
<span class="line"><span>dkjson uses it to speed up decoding.</span></span>
<span class="line"><span>  </span></span>
<span class="line"><span>License: 	MIT/X11</span></span>
<span class="line"><span>Homepage: 	http://dkolf.de/src/dkjson-lua.fsl/</span></span>
<span class="line"><span>Installed in: 	/usr/local</span></span>
<span class="line"><span>  </span></span>
<span class="line"><span>Modules:</span></span>
<span class="line"><span>	dkjson (/usr/local/share/lua/5.3/dkjson.lua)</span></span></code></pre></div><p>It presents a short description of the rock, its license, and the list of modules it provides (in this case, only one, <code>dkjson</code>). Note that &quot;Installed in:&quot; shows the directory tree where the rock was installed. This is the &quot;rocks tree&quot; in use.</p><p>Most LuaRocks installations will feature two rocks trees:</p><ul><li>&quot;system&quot; <a href="./rocks_repositories.html">rock tree</a> (used by default)</li><li>&quot;user&quot; <a href="./rocks_repositories.html">rock tree</a></li></ul><p>To be able to use the module, we need to make sure that Lua can find that dkjson.lua file when we run <code>require(&quot;dkjson&quot;)</code>. You can check your Lua paths from the Lua environment, using</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>print(package.path)</span></span>
<span class="line"><span>print(package.cpath)</span></span></code></pre></div><p>These variables can be pre-configured from outside Lua, using the LUA_PATH and LUA_CPATH environment variables.</p><p>If you installed both Lua and LuaRocks in their default directories (/usr/local on Linux and Mac OSX), then the &quot;system&quot; tree is /usr/local and it will work by default. However, the &quot;user&quot; tree (for installing rocks without admin privileges) is not detected by Lua by default. For that we&#39;ll need to configure these environment variables.</p><p>LuaRocks offers a semi-automated way to do this. If you type the following command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks path --bin</span></span></code></pre></div><p>...it will print commands suitable for your platform for setting up your environment. On typical Unix terminal environments, you can type this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>eval &quot;$(luarocks path --bin)&quot;</span></span></code></pre></div><p>and it apply the changes, temporarily, to your shell. To have these variables set permanently, you have to configure the environment variables to your shell configuration (for example, by adding the above line to your <code>.bashrc</code> file if your shell is Bash).</p><h1 id="multiple-versions-using-the-luarocks-package-loader" tabindex="-1">Multiple versions using the LuaRocks package loader <a class="header-anchor" href="#multiple-versions-using-the-luarocks-package-loader" aria-label="Permalink to &quot;Multiple versions using the LuaRocks package loader&quot;">​</a></h1><p>If you want to make use of LuaRocks&#39; support for multiple installed versions of modules, you need to load a custom package loader: luarocks.loader.</p><p>You should be able to launch the Lua interpreter with the LuaRocks-enabled loader by typing:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lua -lluarocks.loader</span></span></code></pre></div><p>Alternatively, you can load the LuaRocks module loader from Lua by issuing this command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>require &quot;luarocks.loader&quot;</span></span></code></pre></div><p>If your system is correctly set up so that this command runs with no errors, subsequent calls to <code>require()</code> are LuaRocks-aware and the exact version of each module will be determined based on the dependency tree of previously loaded modules.</p><h1 id="scripts-installed-by-rocks-and-the-scripts-path" tabindex="-1">Scripts installed by rocks (and the scripts path) <a class="header-anchor" href="#scripts-installed-by-rocks-and-the-scripts-path" aria-label="Permalink to &quot;Scripts installed by rocks (and the scripts path)&quot;">​</a></h1><p>Besides modules, rocks can also install command-line scripts. The default location of this directory (unless you configured your local repository differently) is /usr/local/bin for system-wide installs and ~/.luarocks/bin for per-user installs on Unix and %APPDATA%/luarocks/bin on Windows -- make sure it is in your PATH as well.</p><p>If you use the <code>--bin</code> argument in <code>luarocks path</code>, it will also print the appropriate PATH configuration:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks path --bin</span></span></code></pre></div><h1 id="using-in-unix-systems-with-sudo" tabindex="-1">Using in Unix systems with sudo <a class="header-anchor" href="#using-in-unix-systems-with-sudo" aria-label="Permalink to &quot;Using in Unix systems with sudo&quot;">​</a></h1><p>When you use LuaRocks to install a package while you aren&#39;t root, the package will get installed in $HOME/.luarocks/ instead of the system-wide (by default, /usr/local/) and become only available for you. Moreover Lua doesn&#39;t know with its default setup that packages can be available in the current user&#39;s home. If you want to install a package available for all users, you should run it as superuser, typically using sudo.</p><p>For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo luarocks install stdlib</span></span></code></pre></div><p>After that, some files may not have correct permissions. For example, if /usr/local/share/lua/5.1/base.lua is only readable by root user, you should at least set them to readable for all users (chmod a+r or chmod 644).</p><p>For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cd /usr/local/share/lua/5.1</span></span>
<span class="line"><span> sudo chmod a+r *</span></span></code></pre></div><h1 id="using-a-c-compiler" tabindex="-1">Using a C compiler <a class="header-anchor" href="#using-a-c-compiler" aria-label="Permalink to &quot;Using a C compiler&quot;">​</a></h1><p>Because rocks are generally available in the repository as <a href="./types_of_rocks.html">source rocks</a> rather than binary rocks, it is best to have a C compiler available.</p><p>On Windows, MinGW and Microsoft compilers are supported. The compiler should be in the system path, or explicitly configured in the LuaRocks config files. On Windows systems, one way of getting the compiler in the system path is to open the appropriate command prompt as configured by your compiler package (for example, the MSVC Command Prompt for Visual Studio).</p><p>Note that for compiling binary rocks that have dependencies on other libraries, LuaRocks needs to be able to find <a href="./paths_and_external_dependencies.html">external dependencies</a>.</p>`,51)])])}const m=e(n,[["render",i]]);export{h as __pageData,m as default};
