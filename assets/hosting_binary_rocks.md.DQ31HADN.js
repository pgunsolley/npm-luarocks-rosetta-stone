import{_ as e,o as r,c as a,a0 as t}from"./chunks/framework.CNDWyMYS.js";const m=JSON.parse('{"title":"Hosting binary rocks","description":"","frontmatter":{},"headers":[],"relativePath":"hosting_binary_rocks.md","filePath":"hosting_binary_rocks.md"}'),s={name:"hosting_binary_rocks.md"};function i(c,o,n,d,l,p){return r(),a("div",null,[...o[0]||(o[0]=[t('<h1 id="hosting-binary-rocks" tabindex="-1">Hosting binary rocks <a class="header-anchor" href="#hosting-binary-rocks" aria-label="Permalink to &quot;Hosting binary rocks&quot;">​</a></h1><p>At the moment, LuaRocks.org hosts only rockspecs and src.rock files, and not binary rocks. But you can host your own repository of binary rocks at any static HTTP server (for example, Github Pages).</p><p>Suppose you want to host a binary rock for a rock of yours named <code>my_rock</code>:</p><p>Step 1: build your rocks with <code>luarocks build my_rock</code>. This will compile your rocks locally in your system. After a successful build, you should see them installed with <code>luarocks list</code>.</p><p>Step 2: create binary rocks from using <code>luarocks pack my_rock</code>. This will create a binary rock file, named after your operating system and processor, for example, <code>my_rock-1.0-1-macosx-x86_64.rock</code>. Note that this binary is dependent on library versions of your own machine. This is more of an issue in some operating systems than others (Linux binaries are very picky with library dependencies, so it&#39;s helpful to build binaries on older distros for greater compatibility.)</p><p>Step 2.5: if your rock has dependencies, repeat step 2 for them as well.</p><p>Step 3: create a new directory <code>my_dir</code>, copy your rock binary into it (and possibly your rockspec and src.rock as well, for a nice one-stop-shop of your rock) and run <code>luarocks-admin make-manifest my_dir</code>. This will create files named <code>manifest-*</code> in it, which turn this directory into a working LuaRocks server. You can even use it locally: <code>luarocks install --server=my_dir my_rock</code> should work!</p><p>Step 4: upload the contents of <code>my_dir</code>, manifest files and rocks, into a HTTP server and use its URL as the argument of <code>--server</code>. For example, if you uploaded it into <code>http://example.com/binary-rock/manifest-5.3</code> and <code>http://example.com/binary-rock/my-rock-1.0-1-macosx-x86_64.rock</code>, then using <code>luarocks install --server=http://example.com/binary-rock my_rock</code> should fetch the manifest, read it, find the rock name, download it and install it.</p><p>Note that in a repo that contains both binary and source rocks, running <code>luarocks install http://example.com/binary-rock my_rock</code> will download and install the binary rock, and <code>luarocks build http://example.com/binary-rock my_rock</code> will download, compile and install the source rock.</p>',9)])])}const k=e(s,[["render",i]]);export{m as __pageData,k as default};
