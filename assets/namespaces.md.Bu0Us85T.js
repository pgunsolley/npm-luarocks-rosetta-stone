import{_ as a,o,c as s,a0 as t}from"./chunks/framework.CNDWyMYS.js";const u=JSON.parse('{"title":"Namespaces","description":"","frontmatter":{},"headers":[],"relativePath":"namespaces.md","filePath":"namespaces.md"}'),n={name:"namespaces.md"};function r(c,e,i,l,d,m){return o(),s("div",null,[...e[0]||(e[0]=[t('<h1 id="namespaces" tabindex="-1">Namespaces <a class="header-anchor" href="#namespaces" aria-label="Permalink to &quot;Namespaces&quot;">​</a></h1><p>The LuaRocks.org repository allows for each user to published their own collection of rocks. Since LuaRocks 3, this concept of per-user rocks has been integrated into the tool, in the form of <strong>namespaces</strong>.</p><p>What you can do with namespaces:</p><ul><li>Install packages using a namespace: <ul><li><code>luarocks install my_user/my_rock</code></li></ul></li><li>Depend on a specific namespaced version of a rock in your rockspec: <ul><li><code>dependencies = { &quot;my_user/my_rock &gt; 2.0&quot; }</code></li></ul></li></ul><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>LuaRocks has always supported multiple repositories, which can be set in the <a href="./config_file_format.html">config file</a> with the <code>rocks_servers</code> entry. A repository is an address (a local directory or a remote URL) where LuaRocks can find a <code>manifest-5.x</code> file and .rock and .rockspec files. We also call such repository a &quot;manifest&quot;, for short.</p><p>The <a href="https://luarocks.org" target="_blank" rel="noreferrer">LuaRocks.org</a> website features a root manifest at <code>https://luarocks.org</code> as well as per-user manifests at <code>https://luarocks.org/manifests/&lt;your-user-name&gt;</code>. Entries in the root manifest are operated in a first-come first-served manner, but even if someone else has already taken a rock name, you can upload your own version of it to your user manifest. You can refer to a per-user manifest the same way as any other rocks server, adding to your configuration or using it with the<code>--server</code> flag. This means that you were always able to install your own version of a rock using a command such as <code>luarocks install &lt;my-rock&gt; --server=https://luarocks.org/manifests/&lt;your-user-name&gt;</code>. However, you could not specifically depend on it from another rockspec, and once installed, the information that this rock came from a specific manifest was lost. With namespaces, now you can!</p><h2 id="using-namespaces" tabindex="-1">Using namespaces <a class="header-anchor" href="#using-namespaces" aria-label="Permalink to &quot;Using namespaces&quot;">​</a></h2><p>All <code>luarocks</code> commands that accept a rock name as command line argument can now take a namespaced variant:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>luarocks install my_user/my_rock</span></span></code></pre></div><p>LuaRocks will take your <code>rocks_trees</code> configuration and search for namespaced manifests on each entry: for example, given the default server <code>https://luarocks.org</code> it will look in <code>https://luarocks.org/manifests/my_user</code>.</p><p>When installing, LuaRocks will internally store the information that this copy of <code>my_rock</code> came from the <code>my_user</code> namespace, so it will be able to use that information when another rockspec specifically asks for <code>my_user/my_rock</code> in its dependencies. (The namespace information is stored in a separate <code>rock_namespace</code> metadata file, at <code>lib/luarocks/rocks-5.x/my_rock/1.0-1/rock_namespace</code>, relative to your local rocks tree.)</p><h2 id="compatibility-between-non-namespaced-and-namespaced-rocks" tabindex="-1">Compatibility between non-namespaced and namespaced rocks <a class="header-anchor" href="#compatibility-between-non-namespaced-and-namespaced-rocks" aria-label="Permalink to &quot;Compatibility between non-namespaced and namespaced rocks&quot;">​</a></h2><p>A namespaced package can stand for a non-namespaced one. If you have <code>my_user/my_rock 1.0</code> installed and a rock depends on <code>my_rock 1.0</code>, the installed rock will satisfy the dependency.</p><p>The opposite is not true: if you have <code>my_rock 1.0</code> installed but that did not come from a <code>my_user</code> namespace, it will not satisfy a dependency for <code>my_user/my_rock</code>.</p><h2 id="current-limitations" tabindex="-1">Current limitations <a class="header-anchor" href="#current-limitations" aria-label="Permalink to &quot;Current limitations&quot;">​</a></h2><p>You cannot have two rocks with the same name and version but different namespaces installed at the same time in the same local rocks tree.</p>',17)])])}const h=a(n,[["render",r]]);export{u as __pageData,h as default};
