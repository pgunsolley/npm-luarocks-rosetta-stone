import{_ as i,C as a,o as s,c as r,a0 as o,E as l,w as n,a as c}from"./chunks/framework.CNDWyMYS.js";const k=JSON.parse('{"title":"Config file format","description":"","frontmatter":{},"headers":[],"relativePath":"config_file_format.md","filePath":"config_file_format.md"}'),d={name:"config_file_format.md"};function u(p,e,h,f,m,b){const t=a("font");return s(),r("div",null,[e[1]||(e[1]=o('<h1 id="config-file-format" tabindex="-1">Config file format <a class="header-anchor" href="#config-file-format" aria-label="Permalink to &quot;Config file format&quot;">​</a></h1><p>The LuaRocks configuration file is a Lua file containing assignments. Default values are assumed for variables that are not assigned explicitly.</p><p>The location of the configuration file can be configured through flags during installation. If no <code>--force-config</code> or <code>/FORCECONFIG</code> flags were used on installation, the <code>LUAROCKS_CONFIG</code> environment variable can be used as well (or for a particular Lua version, <code>LUAROCKS_CONFIG_5_2</code>, similar to the <code>LUA_PATH_5_2</code> feature introduced in Lua 5.2).</p><p>The default value is platform-dependent; exact paths depend on flags given during installation, but on Unix systems it would be something like <code>/etc/luarocks/config.lua</code> or <code>/etc/luarocks/config-5.1.lua</code> for older versions of lua; on Windows systems, <code>c:\\luarocks\\config.lua</code>.</p><p>Specifically on Unix systems, the following directories are searched (in this order) for a user config file:</p><ul><li><code>$XDG_CONFIG_HOME/luarocks/</code>.</li><li><code>$HOME/.config/luarocks/</code>.</li><li><code>$HOME/.luarocks/</code>.</li></ul><h1 id="locations" tabindex="-1">Locations <a class="header-anchor" href="#locations" aria-label="Permalink to &quot;Locations&quot;">​</a></h1><ul><li><p><code>rocks_trees</code> (array of strings or tables) - The path to the local rocks trees, where rocks are installed. When installing rocks, LuaRocks tries to pick a location to store the rock starting from the bottom of the list; when loading rocks in runtime, LuaRocks scans from the top of the list. This way, if one has the local dir first and the system-wide dir last (this is what the default installation does, for instance), rocks installed by the user take precedence over rocks installed system-wide, and rocks installed by the admin user go to the system-wide dir while user installations go to their home directory. An entry in this table can be:</p><ul><li>a string, denoting the prefix of a tree. For example: <code>home..&quot;/.luarocks&quot;</code></li><li>a table, where deployment subdirectories of a tree can be further customized. The &#39;root&#39; field is mandatory; &#39;bin_dir&#39;, &#39;lib_dir&#39; and &#39;lua_dir&#39; are optional. Example: <code>{ root = home..&quot;/local&quot;, bin_dir = home..&quot;/local/arch/bin&quot;, lib_dir = home..&quot;/local/arch/lib/lua/5.1&quot;, lua_dir = home..&quot;/local/share/lua/5.1&quot; }</code></li></ul></li><li><p><code>rocks_servers</code> (array of strings) - Remote URLs or local pathnames of rocks servers: directories containing .rock or .rockspec files, and a &quot;manifest&quot; file, generated by the luarocks-admin make_manifest command. Default is { &quot;<a href="http://luarocks.org/repositories/rocks" target="_blank" rel="noreferrer">http://luarocks.org/repositories/rocks</a>&quot; }.</p></li><li><p><code>external_deps_dirs</code> (array of strings) - Where to look for external dependencies, when a prefix is not set for a specific dependency in the <em>variables</em> table (see below) or through the command-line. Default is { &quot;/usr/local&quot;, &quot;/usr&quot; } on Unix; { &quot;c:\\external&quot; } on Windows.</p></li><li><p><code>external_deps_subdirs</code> (table with string keys and string values) - Subdirectories to be used in conjunction with external_deps_dirs. Specifies where to look for specific types external dependencies. This can be overriden, for example, on Linux distributions which feature multiarch libraries and libraries are no longer in the &quot;lib&quot; subdir. Default is { bin = &quot;bin&quot;, lib = &quot;lib&quot;, include = &quot;include&quot; }.</p></li><li><p><code>runtime_external_deps_subdirs</code> (table with string keys and string values) - Same as the above, to be used with <em>luarocks install</em>.</p></li><li><p><code>lib_modules_path</code> (string) - The path where modules with native C extensions will be installed. If you are using a x86_64 *nix OS, you will probably need this line in <code>config.lua</code>: <code>lib_modules_path=&quot;/lib64/lua/&quot;..lua_version</code>. See <a href="https://github.com/keplerproject/luarocks/issues/416" target="_blank" rel="noreferrer">issue 416</a>.</p></li></ul><h1 id="file-upload" tabindex="-1">File upload <a class="header-anchor" href="#file-upload" aria-label="Permalink to &quot;File upload&quot;">​</a></h1><ul><li><p><code>upload_server</code> (string) - An FTP URL for a rock server (optionally including username and password), or an alias specified in the upload_servers table. Example: <code>&quot;ftp://_user_:_password_@ftp.luarocks.org/repositories/rocks&quot;</code></p></li><li><p><code>upload_user</code> (string) - Default login name rock servers.</p></li><li><p><code>upload_password</code> (string) - Default password rock servers.</p></li><li><p><code>upload_servers</code> (table with string keys and table values) - A list of aliases for URLs, to be used for accessing repositories through various protocols. Each entry has a string alias as a key and a table as a value. This table value contains protocol names as keys and pathnames as values. Protocols &quot;http&quot;, &quot;ftp&quot; and &quot;sftp&quot; are supported. Example: <code>{ rocks = { http = &quot;www.example.com/rocks&quot;, sftp = &quot;example.com/var/rocks&quot; } }</code></p></li></ul><h1 id="platform-specific-settings" tabindex="-1">Platform-specific settings <a class="header-anchor" href="#platform-specific-settings" aria-label="Permalink to &quot;Platform-specific settings&quot;">​</a></h1><ul><li><p><code>lua_extension</code> (string) - Filename extension of Lua files (without the dot/separator). Default is &quot;lua&quot;.</p></li><li><p><code>lib_extension</code> (string) - Filename extension of dynamic library files (without the dot/separator). Default is &quot;so&quot; on Unix; &quot;dll&quot; on Windows.</p></li><li><p><code>arch</code> (string) - A two-part string identifying operating system and hardware architecture, for filtering binary rocks. This value is autodetected by LuaRocks. Example values are &quot;macosx-powerpc&quot; and &quot;win32-x86&quot;.</p></li><li><p><code>platforms</code> (array of string) - A list of string identifiers indicating which platform constraints can be satisfied by the running system. This is used for filtering commands on the LuaRocks build rules. This allows a more general platform definition such as &quot;unix&quot; when the same build commands are valid for all Unix variants, instead of enumerating all known valid arch entries, and at the same time using a more specific definition such as &quot;macosx&quot; when a platform-specific flag is used. This value is automatically filled according to the value of <em>arch</em>.</p></li><li><p><code>external_deps_patterns</code> (table) - Name patterns to be used when matching dependencies in a <a href="./platform_agnostic_external_dependencies.html">portable way</a>.</p></li><li><p><code>runtime_external_deps_patterns</code> (table) - Name patterns to be used by <em>luarocks install</em> when matching dependencies in a <a href="./platform_agnostic_external_dependencies.html">portable way</a>.</p></li><li><p><code>link_lua_explicitly</code> (boolean) - Link the Lua library to the built modules when using the builtin mode (this is set to true for Cygwin).</p></li></ul><h1 id="variables" tabindex="-1">Variables <a class="header-anchor" href="#variables" aria-label="Permalink to &quot;Variables&quot;">​</a></h1><ul><li><code>variables</code> (table) - A table containing string-to-string key-value pairs containing variables to be substituted by build rules in rockspecs. LuaRocks provides a general facility for build back-ends in which they can substitute the entries of this table in strings containing references written $(LIKE_THIS). Some standard variables are expected by the included back-ends. For example, the &quot;make&quot; back-end expects the LIBFLAG to contain the flag to be passed to the C compiler to instruct it to build a shared library. So, in Linux systems, <code>variables[&quot;LIBFLAG&quot;] = &quot;-shared&quot;</code>.</li></ul><p>After reading the user entries, LuaRocks dynamically adds entries to this table that refer to the rock being compiled, to make values available to the build back-end. These are:</p><ul><li><p><code>PREFIX</code> - The installation prefix of the rock (absolute path inside the rocks tree). Example: <code>&quot;/home/hisham/.luarocks/5.1/foo/1.0-1/&quot;</code></p><ul><li><p><code>LUADIR</code> - Directory for storing Lua modules written in Lua (absolute path inside the rock entry of the rocks tree). Example: <code>&quot;/home/hisham/.luarocks/5.1/foo/1.0-1/lua/&quot;</code></p></li><li><p><code>LIBDIR</code> - Directory for storing Lua modules written in C (absolute path inside the rock entry of the rocks tree). Example: <code>&quot;/home/hisham/.luarocks/5.1/foo/1.0-1/lib/&quot;</code></p></li><li><p><code>BINDIR</code> - Directory for storing command-line scripts (absolute path inside the rock entry of the rocks tree). Example: <code>&quot;/home/hisham/.luarocks/5.1/foo/1.0-1/bin/&quot;</code></p></li><li><p><code>CONFDIR</code> - Directory for storing configuration files for a module (absolute path inside the rock entry of the rocks tree). Example: <code>&quot;/home/hisham/.luarocks/5.1/foo/1.0-1/conf/&quot;</code></p></li></ul></li></ul><p>Such entries should not be added by the user in the configuration file, as they are dynamically constructed for each different rock. If you need to customize the various locations where files are deployed in a tree, use the table syntax for <code>rocks_trees</code> entries (see above).</p>',17)),l(t,{color:"green"},{default:n(()=>[...e[0]||(e[0]=[c("(since 2.0.5)",-1)])]),_:1}),e[2]||(e[2]=o(' You can also override external<p>commands called by LuaRocks by using entries in the <em>variables</em> table, in case you need to customize the way they are called. Note that by installing appropriate Lua modules, most of these external command invocations can be avoided. Currently recognized entries in the <em>variables</em> table are:</p><ul><li><p><code>MAKE</code> for build.type=&quot;make&quot;;</p></li><li><p><code>CC, LD</code> (and <code>RC</code> on Windows) for build.type=&quot;builtin&quot;;</p></li><li><p><code>CVS, GIT, SSCM, SVN</code> for their respective download protocols;</p></li><li><p><code>RSYNC, SCP</code> for luarocks-admin operations;</p></li><li><p><code>WGET</code> or <code>CURL</code> when LuaSocket is not installed;</p></li><li><p><code>PWD, MKDIR, RMDIR, CP, LS, RM, FIND, TEST, CHMOD, STAT</code> when LuaFileSystem is not installed;</p></li><li><p><code>ZIP</code> when Lua-Zlib is not installed;</p></li><li><p><code>UNZIP</code> when LuaZip is not installed;</p></li><li><p><code>GUNZIP, BUNZIP2, TAR</code> on Unix or <code>SEVENZ</code> on Windows for source extraction during &quot;luarocks build&quot;;</p></li><li><p><code>MD5SUM, OPENSSL</code> or <code>MD5</code> according to the operating system, when the Lua md5 module is not installed.</p></li></ul><h1 id="external-input" tabindex="-1">External input <a class="header-anchor" href="#external-input" aria-label="Permalink to &quot;External input&quot;">​</a></h1><p>As the config file itself is a Lua code file, there is some possibility to execute Lua code. Because it is run in a sandbox this is very limited, but might still be useful.</p><p>What LuaRocks makes available:</p><ul><li><p>parameters/values: several values are made available to the config file (pre-loaded in the sandbox) and include things like processor and OS platform, rocks trees which are in use, etc. (see <code>dump_env()</code> below)</p></li><li><p>function: <code>os_getenv(varname)</code>; this is the regular Lua function <code>os.getenv()</code> and allows one to fetch environment variable values from the OS.</p></li><li><p>function: <code>dump_env()</code>; this will dump a list of all variables provided by LuaRocks as a debug aid.</p></li></ul><p>To test this, add a line <code>dump_env()</code> to your config file and execute <code>luarocks</code> on the commandline to see the results.</p><h1 id="other" tabindex="-1">Other <a class="header-anchor" href="#other" aria-label="Permalink to &quot;Other&quot;">​</a></h1><ul><li><p><code>cmake_generator</code> (string) - If specified it overrides the default cmake generator. Currently only Makefile-based generators are supported.</p></li><li><p><code>wrap_bin_scripts</code> (boolean) - The default value is true: scripts installed at bin/ are launched by a wrapper script that sets path environment variables to ensure Lua modules are found. If set to false, scripts installed at bin/ are copied directly, and no wrappers are generated.</p></li><li><p><code>use_extensions</code> (boolean) - If specified, rockspec format verison 1.1 is enabled, adding the deploy.wrap_bin_scripts option to the rockspec format, which acts like the wrap_bin_scripts option above, in a rock by rock basis.</p></li><li><p><code>local_by_default</code> (boolean) - If <code>true</code>, the tree in the user&#39;s home directory is used as if the command line option <code>--local</code> had been given</p></li></ul>',10))])}const _=i(d,[["render",u]]);export{k as __pageData,_ as default};
