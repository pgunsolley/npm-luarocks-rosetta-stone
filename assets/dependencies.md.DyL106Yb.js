import{_ as o,o as t,c as n,a0 as a}from"./chunks/framework.CNDWyMYS.js";const p=JSON.parse('{"title":"Dependencies","description":"","frontmatter":{},"headers":[],"relativePath":"dependencies.md","filePath":"dependencies.md"}'),r={name:"dependencies.md"};function s(i,e,d,c,l,u){return t(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#dependencies" aria-label="Permalink to &quot;Dependencies&quot;">​</a></h1><p>LuaRocks handles dependencies on Lua modules — rocks can specify other rocks it depends on, and attempts to fulfill those dependencies at install time. A rock will only be installed if all its dependencies can be fulfilled.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p><strong>Node.js Developer Quick Tip</strong>:</p><ul><li>Like <code>dependencies</code> in <code>package.json</code>, rockspecs specify runtime dependencies in the <code>dependencies</code> list.</li><li>While npm downloads a nested tree of dependencies into a single local <code>node_modules/</code> folder, LuaRocks resolves dependencies globally (or in a flat structure inside the designated local tree).</li><li>The <code>test_dependencies</code> block in a rockspec is equivalent to <code>devDependencies</code> in <code>package.json</code> (such as testing frameworks like <code>busted</code> or build tools).</li></ul><p>For a detailed guide on how to isolate dependencies in Lua (equivalent to <code>node_modules</code>), check the <a href="./luarocks_for_npm_users.html">LuaRocks for npm Developers Guide</a>.</p></div><p>LuaRocks also supports verification of dependencies on external libraries. A rock can specify an external package it depends on (for example, a C library), and give to LuaRocks hints on how to detect if it is present, typically as C header or library filenames. LuaRocks then looks for these files in a pre-configured search path and, if found, assumes the dependency is fulfilled. If not found, an error message is reported and the user can then install the missing external dependency (using the tools provided by their operating system) or inform LuaRocks of the location of the external dependency in case it was installed and LuaRocks failed to find it.</p><p>Dependencies of a rock are specified in its <a href="./rockspec_format.html">rockspec</a> file. See the complete specification of the dependency syntax in the <a href="./rockspec_format.html">Rockspec format</a> page and examples in rockspec files of the <a href="https://luarocks.org" target="_blank" rel="noreferrer">public rocks server</a>.</p><h1 id="dependency-modes" tabindex="-1">Dependency modes <a class="header-anchor" href="#dependency-modes" aria-label="Permalink to &quot;Dependency modes&quot;">​</a></h1><p>Since 2.0.12, the LuaRocks command-line tool supports different &quot;dependency modes&quot;. These are useful to specify how it should behave on the presence of multiple rocks trees specified in the <a href="./config_file_format.html">config file</a>:</p><ul><li><code>one</code></li><li><code>all</code></li><li><code>order</code></li><li><code>none</code></li></ul><p>This can be set through the configuration file, using the string variable deps_mode (example: <code>deps_mode=&quot;order&quot;</code>) or through the command-line, using the <code>--deps-mode</code> flag (example: <code>--deps-mode=order</code>).</p><h2 id="one" tabindex="-1">one <a class="header-anchor" href="#one" aria-label="Permalink to &quot;one&quot;">​</a></h2><p>This is the default behavior. LuaRocks only takes <strong>one</strong> rocks tree into account when checking dependencies. For example, if you have two rocks trees configured (<code>rocks_trees={home..&quot;/.luarocks&quot;, &quot;/usr&quot;}</code>) and you try to install a rock in <code>$HOME/.luarocks</code>, it will check that all required dependencies are installed <em>in that tree</em>. If the dependency rock is already installed under <code>/usr</code>, it will ignore that copy.</p><p>This is a cautious behavior because it ensures that a rock and all its dependencies are installed under the same tree. So, if another user modifies the other tree, there&#39;s no risk that the rock installed in your home tree might stop working.</p><h2 id="all" tabindex="-1">all <a class="header-anchor" href="#all" aria-label="Permalink to &quot;all&quot;">​</a></h2><p>LuaRocks scans <strong>all</strong> configured rocks trees to search for dependencies. If the required rock for a dependency is available in <em>any</em> tree, it will consider that dependency fulfilled, and will not install that again.</p><p>However, note for example that if you install a rock in /usr and its dependency was installed in your $HOME tree, the installed rock will work for your user account (which has access to the /usr tree and your home tree), but will probably not work for other users, if they don&#39;t have a compatible dependency installed in their own home trees.</p><h2 id="order" tabindex="-1">order <a class="header-anchor" href="#order" aria-label="Permalink to &quot;order&quot;">​</a></h2><p>LuaRocks uses the <strong>order</strong> of the list of rocks trees to determine if a rocks tree should be used as a valid provider of dependencies or not. LuaRocks will only use rocks from either the tree it is installing to, or trees that appear <strong>below</strong> the rock that&#39;s in use in the rocks_trees array. So, if your rocks_trees array looks like <code>{home..&quot;/.luarocks&quot;, &quot;/usr/local&quot;, &quot;/usr&quot;}</code>, installing a rock under your $HOME directory will accept dependencies from any of the three trees. Installing into <code>/usr/local</code> will use dependencies from <code>/usr</code> but not from the <code>$HOME</code> directory. Installing into <code>/usr</code> will use rocks from that tree only.</p><p>So, by carefully ordering your array of rocks trees in the configuration file, you can use the same configuration file for both your administrator account and your regular user account.</p><p>Note, however, that like in the &quot;all&quot; mode, an administrator can break a rock you installed in your home account by removing a dependency rock from the global tree.</p><h2 id="none" tabindex="-1">none <a class="header-anchor" href="#none" aria-label="Permalink to &quot;none&quot;">​</a></h2><p>LuaRocks does not use any tree, or install any dependencies. This means, of course, that installed rocks may be installed with missing dependencies and may simply not work. This mode is not recommended for general use, but it is useful in some specific scenarios (incorrect dependencies, batch recompilation, etc.)</p><p>This is equivalent to the old <code>--nodeps</code> option.</p>',22)])])}const f=o(r,[["render",s]]);export{p as __pageData,f as default};
